name: Build binaries

on:
  workflow_dispatch: {}
  release:
    types: [published]

jobs:
  prepare-manual-release:
    name: Prepare release for manual build
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      tag: ${{ steps.release.outputs.tag }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Find or create release
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          tag="$(gh release list --repo "$GITHUB_REPOSITORY" --limit 1 --exclude-drafts --exclude-pre-releases --json tagName --jq '.[0].tagName')"
          if [ -z "$tag" ]; then
            tag="build-${GITHUB_RUN_NUMBER}"
            gh release create "$tag" \
              --repo "$GITHUB_REPOSITORY" \
              --title "Build ${GITHUB_RUN_NUMBER}" \
              --notes "Automatic build from ${GITHUB_SHA}." \
              --target "${GITHUB_SHA}"
            echo "Created release $tag"
          else
            echo "Using latest release $tag"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

  # Manual runs: one matrix job per CLI binary.
  build-cli:
    name: Build CLI (${{ matrix.target }})
    if: github.event_name == 'workflow_dispatch'
    needs: prepare-manual-release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: windows-amd64
            make_target: windows-amd64
            asset_target: cli-asset-windows-amd64
          - target: windows-arm64
            make_target: windows-arm64
            asset_target: cli-asset-windows-arm64
          - target: linux-amd64
            make_target: linux-amd64
            asset_target: cli-asset-linux-amd64
          - target: linux-arm64
            make_target: linux-arm64
            asset_target: cli-asset-linux-arm64
          - target: linux-armv7
            make_target: linux-armv7
            asset_target: cli-asset-linux-armv7
          - target: linux-mipsle
            make_target: linux-mipsle
            asset_target: cli-asset-linux-mipsle
          - target: linux-mips
            make_target: linux-mips
            asset_target: cli-asset-linux-mips
          - target: darwin-amd64
            make_target: darwin-amd64
            asset_target: cli-asset-darwin-amd64
          - target: darwin-arm64
            make_target: darwin-arm64
            asset_target: cli-asset-darwin-arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Build CLI
        run: make ${{ matrix.make_target }}

      - name: Hash artifact
        id: stage
        run: |
          set -euo pipefail
          asset="$(make -s ${{ matrix.asset_target }})"
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
          sha256sum "$asset"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sni-spoofing-${{ matrix.target }}
          path: ${{ steps.stage.outputs.asset }}

      - name: Upload binary to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          gh release upload "${{ needs.prepare-manual-release.outputs.tag }}" "${{ steps.stage.outputs.asset }}" --repo "$GITHUB_REPOSITORY" --clobber

  # Releases: build every CLI target and upload to the release that triggered the workflow.
  build-cli-release:
    name: Build CLI (all platforms, release)
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Build all CLI targets
        run: make dist

      - name: Create checksums
        run: make dist-checksums

      - name: Upload assets to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${{ github.event.release.tag_name }}" dist/* --repo "$GITHUB_REPOSITORY" --clobber

  build-gui:
    name: Build GUI (${{ matrix.target }})
    if: github.event_name == 'workflow_dispatch'
    needs: prepare-manual-release
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-amd64
            runner: ubuntu-latest
            make_target: gui-linux-amd64
            asset_target: gui-asset-linux-amd64
          - target: linux-arm64
            runner: ubuntu-24.04-arm
            make_target: gui-linux-arm64
            asset_target: gui-asset-linux-arm64
          - target: windows-amd64
            runner: ubuntu-latest
            make_target: gui-windows-amd64
            asset_target: gui-asset-windows-amd64
          - target: windows-arm64
            runner: ubuntu-latest
            make_target: gui-windows-arm64
            asset_target: gui-asset-windows-arm64
          - target: darwin-universal
            runner: macos-latest
            make_target: gui-darwin-universal
            asset_target: gui-asset-darwin-universal
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: gui/frontend/package-lock.json

      - name: Install Linux GUI build deps
        if: runner.os == 'Linux' && (matrix.make_target == 'gui-linux-amd64' || matrix.make_target == 'gui-linux-arm64')
        run: make deps-linux

      - name: Build GUI
        run: make ${{ matrix.make_target }}

      - name: Hash artifact
        id: stage
        run: |
          set -euo pipefail
          asset="$(make -s ${{ matrix.asset_target }})"
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
          if command -v sha256sum >/dev/null; then
            sha256sum "$asset"
          else
            shasum -a 256 "$asset"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sni-spoofing-gui-${{ matrix.target }}
          path: ${{ steps.stage.outputs.asset }}

      - name: Upload GUI binary to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          gh release upload "${{ needs.prepare-manual-release.outputs.tag }}" "${{ steps.stage.outputs.asset }}" --repo "$GITHUB_REPOSITORY" --clobber

  build-gui-release:
    name: Build GUI (${{ matrix.target }}) for release
    if: github.event_name == 'release'
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-amd64
            runner: ubuntu-latest
            make_target: gui-linux-amd64
            asset_target: gui-asset-linux-amd64
          - target: linux-arm64
            runner: ubuntu-24.04-arm
            make_target: gui-linux-arm64
            asset_target: gui-asset-linux-arm64
          - target: windows-amd64
            runner: ubuntu-latest
            make_target: gui-windows-amd64
            asset_target: gui-asset-windows-amd64
          - target: windows-arm64
            runner: ubuntu-latest
            make_target: gui-windows-arm64
            asset_target: gui-asset-windows-arm64
          - target: darwin-universal
            runner: macos-latest
            make_target: gui-darwin-universal
            asset_target: gui-asset-darwin-universal
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: gui/frontend/package-lock.json

      - name: Install Linux GUI build deps
        if: runner.os == 'Linux' && (matrix.make_target == 'gui-linux-amd64' || matrix.make_target == 'gui-linux-arm64')
        run: make deps-linux

      - name: Build GUI
        run: make ${{ matrix.make_target }}

      - name: Hash artifact
        id: stage
        run: |
          set -euo pipefail
          asset="$(make -s ${{ matrix.asset_target }})"
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
          if command -v sha256sum >/dev/null; then
            sha256sum "$asset"
          else
            shasum -a 256 "$asset"
          fi

      - name: Upload GUI binary to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${{ github.event.release.tag_name }}" "${{ steps.stage.outputs.asset }}" --repo "$GITHUB_REPOSITORY" --clobber

  build-and-push-image:
    name: Build and push multi-arch Docker image
    runs-on: ubuntu-latest
    needs: build-cli-release
    if: always() && (needs.build-cli-release.result == 'success' || needs.build-cli-release.result == 'skipped')
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for release
        if: github.event_name == 'release'
        id: meta-release
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/sni-spoofing-go
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest

      - name: Extract metadata (tags, labels) for manual trigger
        if: github.event_name == 'workflow_dispatch'
        id: meta-manual
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/sni-spoofing-go
          tags: |
            type=raw,value=${{ github.ref_name }}-{{sha}}

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ github.event_name == 'release' && steps.meta-release.outputs.tags || steps.meta-manual.outputs.tags }}
          labels: ${{ github.event_name == 'release' && steps.meta-release.outputs.labels || steps.meta-manual.outputs.labels }}
